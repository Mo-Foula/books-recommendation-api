import { Controller } from '@nestjs/common'
@Controller('users-books-readings')
export class UsersBooksReadingsController {
  // constructor(private readonly usersBooksReadingsService: UsersBooksReadingsService) {}
  // @Post()
  // create(@Body() createUsersBooksReadingDto: CreateUsersBooksReadingDto) {
  //   return this.usersBooksReadingsService.create(createUsersBooksReadingDto);
  // }
  // @Get()
  // findAll() {
  //   return this.usersBooksReadingsService.findAll();
  // }
  // @Get(':id')
  // findOne(@Param('id') id: string) {
  //   return this.usersBooksReadingsService.findOne(+id);
  // }
  // @Patch(':id')
  // update(@Param('id') id: string, @Body() updateUsersBooksReadingDto: UpdateUsersBooksReadingDto) {
  //   return this.usersBooksReadingsService.update(+id, updateUsersBooksReadingDto);
  // }
  // @Delete(':id')
  // remove(@Param('id') id: string) {
  //   return this.usersBooksReadingsService.remove(+id);
  // }
}
